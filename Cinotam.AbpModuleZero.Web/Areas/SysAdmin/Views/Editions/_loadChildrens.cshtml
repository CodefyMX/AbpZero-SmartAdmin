@using Castle.Core.Internal
@model IEnumerable<Cinotam.ModuleZero.AppModule.Features.Dto.FeatureDto>
@if (!Model.IsNullOrEmpty())
{
    <ul>
        @foreach (var child in Model)
        {
            if (child.InputType.Name == "SINGLE_LINE_STRING")
            {

                <li data-append-textbox="true" id="@child.Name" data-value="@child.DefaultValue" class="hide-checkbox" id="@child.Name">
                    <a> @child.Name (@child.DefaultValue)</a>
                    @{ Html.RenderPartial("_loadChildrens", child.ChildFeatures); }
                </li>
                        }
                        else
                        {
                            <li id="@child.Name" data-append-textbox="false" data-value="@child.DefaultValue" id="@child.Name">
                                <a> @child.Name</a>
                                @{ Html.RenderPartial("_loadChildrens", child.ChildFeatures); }
                            </li>
                                        }
                                    }
    </ul>
                                    }
